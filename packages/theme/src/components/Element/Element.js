import { styled } from "style";

/**
 * generic theme-ui compatible component
 * preferable to @theme-ui/components when you want to set __themeKey
 */
const Element = styled("span")();

export default Element;
